<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Khóa học của tôi</title>
  <link rel="stylesheet" href="/css/user-courses_styles.css">
</head>
<body>
  <div class="container">
    <h2>Khóa học của tôi</h2>
    <a href="/courses">Xem tất cả khóa học</a> | <a href="/auth/logout">Đăng xuất</a>
    <% if (courses.length === 0) { %>
      <p>Bạn chưa đăng ký khóa học nào.</p>
    <% } else { %>
      <ul>
        <% courses.forEach(course => { %>
          <li>
            <img src="<%= course.logo %>" alt="<%= course.name %>" style="width:100px;">
            <h3><a href="/courses/<%= course._id %>"><%= course.name %></a></h3>
            <p>Thời gian bắt đầu: <%= new Date(course.startDate).toLocaleDateString() %></p>
            <p>Thời gian học: <%= course.duration %> tháng</p>
            <p>Số người tham gia: <%= course.enrolledCount %></p>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </div>
</body>
</html>